name: online_offline
description: "Flutter package for monitoring internet connectivity in real-time using socket."
version: 1.0.0
homepage: https://github.com/fuadreza/online_offline

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:
